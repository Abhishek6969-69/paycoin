{
  "name": "bank-server-wrapper",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "node run-build.js && (cd bank-server && npm run dev)",
    "build": "node run-build.js",
    "start": "node run-build.js && (cd bank-server && npm run start)"
  }
}